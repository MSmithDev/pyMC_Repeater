#!/bin/sh
set -e

case "$1" in
    purge)
        # Remove user and directories
        if getent passwd pymc-repeater >/dev/null; then
            deluser --system pymc-repeater || true
        fi
        rm -rf /etc/pymc_repeater
        rm -rf /var/log/pymc_repeater
        rm -rf /var/lib/pymc_repeater
        ;;
esac

#DEBHELPER#

exit 0
